# Backlog technique — Culture

> Dette technique et tâches connues, pas encore traitées. Pas chargé automatiquement — consulte ce fichier en début de tâche de planification, ou avant de toucher une zone concernée. Une fois un item résolu, **le supprimer** plutôt que de le marquer « ✅ résolu » (git log garde la trace).
>
> Dernière mise à jour : 17/08/2026

- **`QuestionEditor.tsx` : le composant n'est plus monté nulle part.** Depuis que le parcours est passé à `InlineQuestionEditor` (PR #36), plus personne ne rend l'export par défaut de `tabs/QuestionEditor.tsx` — vérifié le 17/08/2026, aucun `<QuestionEditor` dans `src/`. Le fichier reste malgré tout la **source des types** de tout l'onglet examen (`Question`, `QuestionPart`, `ResponseType`) et de `emptyQuestion`, importés par sept fichiers : on ne peut donc pas simplement le supprimer. À faire : déplacer les types et `emptyQuestion` dans un module qui ne porte que ça (`lib/workshops/examTypes.ts` ou un voisin), puis retirer les ~380 lignes de JSX mortes. En attendant, elles suivent les évolutions de l'éditeur vivant sans que rien ne les vérifie — le menu de libellés du 17/08/2026 y a été repris à l'identique pour cette raison.

- **Intégration Stripe & facturation.** Le webhook `src/app/api/webhooks/stripe/route.ts` est un stub inerte (`{ received: true }` sans traitement). À faire au branchement réel : (1) vérifier la signature Stripe (`stripe.webhooks.constructEvent`) avant tout traitement ; (2) gérer `customer.subscription.created/updated/deleted` + `invoice.payment_failed` → `setUserTier` (`src/lib/subscription.ts`) ; (3) idempotence (un même événement peut être reçu plusieurs fois) ; (4) remplacer le mécanisme temporaire d'activation Premium d'atelier (`activateWorkshopPremium`, mot de passe en dur + allowlist email — voir `.claude/rules/server-architecture.md`) par une activation uniquement après paiement confirmé ; (5) facturation ~3,5€/membre à l'acceptation d'une invitation/demande d'adhésion sur un atelier Premium (point d'ancrage : TODO dans `approveJoinRequest`). Plan détaillé dans la mémoire auto `stripe_integration_plan.md`.

- **Nettoyage des utilisateurs supprimés.** `workshops.created_by` et `workshop_members.user_id` stockent l'ID Clerk en texte brut, sans FK ni cascade. Si un utilisateur qui a créé/rejoint des ateliers est supprimé de Clerk, ses lignes deviennent orphelines (retombe sur `'Utilisateur'` à l'affichage, pas de crash mais atelier « fantôme »). À prévoir : webhook Clerk `user.deleted` qui nettoie/réassigne ces lignes côté Supabase, ou job de nettoyage manuel.

- **Popup d'intro « + nouvel examen » à finaliser.** `ExamenTab.tsx` (`HistoryContent`, état `introOpen`) affiche une popup d'introduction avec 3 illustrations actuellement en placeholders CSS (blob ambre en dégradé radial). À remplacer par de vraies images (260×230, débordant du cadre de ±64px, rotation ±4deg) une fois les fichiers fournis.

- **Typer le client Supabase de bout en bout (`createClient<Database>`).** Le schéma est généré (`src/lib/database.types.ts`), mais `getSupabaseServerClient` (`src/lib/supabase.ts`) renvoie volontairement un client non typé. Le passer à `createClient<Database>(url, key)` donnerait une sécurité de type sur toutes les requêtes `.from()`, mais révèle ~15 incohérences réelles à corriger d'abord : colonnes nullables traitées comme non-null, colonnes `jsonb` castées directement en `string[]` dans `examQuestions.ts`/`lib/workshops/exam.ts`, `role: string` (DB) vs `WorkshopRole` côté code, un `update(...)` typé `Record<string, …>` rejeté. Migration à part entière, plusieurs fichiers d'actions.

- **Transfert de propriété d'atelier.** Spécifié dans `docs/product-spec.md` (« Donner la propriété »), non implémenté à ce jour. `setMemberRole`/`removeMember` existent déjà et posent les bases du modèle de rangs (`src/lib/authz.ts`), mais aucune action ne permet de faire passer le rôle `owner` à un autre membre. **Volet facturation à traiter en même temps** : sur un atelier Premium, c'est le propriétaire qui a enregistré le moyen de paiement (`docs/product-spec.md` § Atelier Premium) — le transfert de propriété doit donc aussi réassigner la relation de facturation Stripe (moyen de paiement / abonnement usage-based par membre, voir `stripe_integration_plan.md` étape 6), pas seulement le rôle `owner` en base. À ne pas implémenter avant le branchement Stripe réel (voir item ci-dessus).

- **Export/passage réel des examens.** L'éditeur d'examen produit un aperçu A4 en direct (`ExamenTab.tsx` → `tabs/examen/GeneratorContent.tsx`), mais l'export PDF, le passage en ligne et le passage projeté ne sont pas implémentés (placeholders retirés faute de fonctionnalité réelle).

- **Écritures optimistes sans gestion d'échec dans le générateur d'examen.** Plusieurs handlers de `tabs/examen/*` (ex. `handleSaveQuestion` dans `ExamenTab.tsx`) mettent à jour l'état local immédiatement puis appellent la server action correspondante en fire-and-forget (`.catch(err => console.error(...))`). Si l'écriture échoue côté serveur (perte de droits gestionnaire, erreur Supabase, coupure réseau), l'utilisateur voit son changement comme enregistré alors qu'il ne l'est pas — le décalage ne se révèle qu'au rechargement de la page, sans explication. Le choix optimiste lui-même est justifié sur cette surface (glisser-déposer, cases à cocher, saisie fréquente — attendre un aller-retour serveur à chaque étape serait perceptiblement saccadé), mais il manque la moitié du pattern : en cas d'échec, afficher une erreur à l'utilisateur **et** annuler le changement local, plutôt que rester silencieux. À étoffer : un helper générique (toast d'erreur + rollback de l'état local sur `.catch`) réutilisable par tous ces handlers plutôt qu'un correctif au cas par cas.

- **Pas de gestion de conflit sur `exam_questions`/`exam_generated`.** Depuis la migration `add_user_scope_to_exam_draft` (le brouillon d'édition est désormais isolé par `(workshop_id, user_id)`), la banque de questions et les examens générés restent volontairement partagés par atelier, sans verrou ni fusion. Si deux gestionnaires éditent la même question ou le même examen généré en même temps, la dernière sauvegarde écrase silencieusement l'autre — cas jugé extrêmement rare (édition simultanée du même objet, pas juste du même atelier), pas traité pour le MVP.

- **Alimenter `brick_mastery` (Taxonomie de Bloom).** La table existe (`brick_mastery` : `brick_id` × `user_id` × `bloom_level` 1–6, créée le 19/07/2026 ; lexique produit/code : « notion », voir `CLAUDE.md` §1) mais rien ne l'écrit ni ne la lit. À faire quand le module Analyse arrivera : (1) décider ce qui fait monter un niveau de Bloom — les deux pièces manquantes sont désormais posées (19/07/2026) : `exam_question_items.bloom_level` porte le niveau **visé** par chaque question (la principale comme les liées), et `exam_question_item_bricks` dit **quelles notions** elle couvre, donc une réponse correcte sait déjà quoi faire monter et jusqu'où ; reste à définir la règle (une réussite suffit ? répétition espacée ? régression en cas d'échec ?) ; (2) écrire la maîtrise à la correction d'une réponse ; (3) l'exposer dans `AnalyseTab`. Voir `docs/product-spec.md` § Briques de connaissance (renommage en « Notions » prévu par T42 du chantier de refonte UI).

- **Exercice : suites à donner.** Le bouton « lancer un exercice » de chaque pot est branché depuis le 19/07/2026 (page `/workshops/{id}/exercise/{chapterId}`, tirage aléatoire parmi les questions de parcours du chapitre). Restent ouverts : (1) **aucune progression n'est enregistrée** — rien n'alimente `brick_mastery` (voir item Bloom ci-dessus), chaque tirage est indépendant et rien ne mémorise les questions déjà réussies ; (2) **correction automatique limitée à QCS/QCM** — les réponses textuelles affichent la réponse attendue sans verdict, faute de règle de comparaison (normalisation ? tolérance ? correction par IA ?) ; (3) les questions restent **saisies à la main** et sans lien avec les notions du chapitre, alors que la cible est de les générer à partir d'elles.

- **Génération du programme par IA (conception faite, code à écrire).** Toute la conception est arrêtée dans `docs/ai-ingestion-plan.md` (20/07/2026) : pas d'API HTTP interne, contrat Zod unique avec références locales, stratégie CAG (pas de RAG, pas de vectorisation pour l'instant), colonne `import_id` pour l'annulation à 24h, quatre boutons « générer par IA » en escalier, trajectoire Claude → DeepSeek → modèles locaux. **Trois prérequis y sont identifiés, à traiter avant l'ingestion** : (1) le basculement chapitre → notions sur les questions (change le modèle de données) ; (2) `emptyQuestion()` génère `'q' + Date.now()`, ce qui provoque des collisions d'identifiants silencieuses dès qu'on crée plusieurs questions dans la même milliseconde ; (3) les invariants métier (QCM ≥ 2 choix, notion du même atelier…) ne sont vérifiés nulle part côté `lib/`. Les questions produit encore ouvertes sont listées au §11 du plan.

- **Renommer les tables `workshop_bricks`/`brick_mastery`/`exam_question_bricks` en `notions`/`notion_mastery`/`exam_question_notions`** (et leurs colonnes `brick_id`) — migration destructive côté Supabase, à faire seulement après déploiement du code renommé (chantier de refonte UI, T2, 08/2026), séquencement expand/contract de `CLAUDE.md` §1. D'ici là, le code porte des commentaires `// table encore nommée bricks en base…` à chaque point de contact.

- **Grille de paliers tarifaires Premium — illustration inerte.** `PremiumSection.tsx` (Réglages → Premium) affiche une grille de tarification dégressive (1er membre à 1€, puis de moins en moins cher), conforme à la maquette mais **volontairement non branchée** (chantier de refonte UI, T32, 08/2026) : le produit réel n'a qu'un tarif à plat (`REAL_PRICE_PER_MEMBER = 3,5 €`, non facturé — Stripe non branché, voir l'item Stripe ci-dessus). À faire quand une vraie grille dégressive sera décidée : soit la brancher sur un modèle de paliers réel, soit la retirer si le tarif à plat reste la politique produit.

- **Gamification V2 non implémentée : notifications, gouttes d'eau, série d'arrosage, XP, temps passé, succès.** Aucune table ne stocke de préférence/flux de notification, de solde de gouttes, de série de jours consécutifs, d'XP, de temps de travail ni de succès. Spécifié dans `docs/product-spec.md` § Gamification (V2+). État actuel — **tout est monté et visible, volontairement non fonctionnel** : cloche de notifications (panneau figé sur deux exemples) et compteur de gouttes (`PLACEHOLDER_DROPLETS = 12`) dans `DashboardHeader.tsx` depuis le 06/08/2026 ; **carrousel de 5 statistiques du profil** (`PLACEHOLDER_STATS` dans `ProfileClient.tsx` : 12 jours d'affilée, 8 420 XP, 47 h, 23 succès, 156 notions maîtrisées) et ligne « notifications · activées » de ses paramètres, depuis le 06/08/2026 également. La tuile « notions maîtrisées » est la seule des 5 qui pourrait être branchée dès aujourd'hui — `brick_mastery.score` existe depuis le 06/08/2026, il suffirait de compter les notions à `score >= 30`. `HAS_STREAK` a disparu, la série est désormais une des 4 tuiles. À brancher sur de vraies données le jour où ces tables existent — ce sont les seuls endroits de l'app connectée qui affichent sciemment une donnée factice.

- **Vocabulaire d'abonnement incohérent : « Smart »/« basique » (profil) vs « Gratuit »/« Premium »/« Premium+ » (partout ailleurs).** L'encart d'upsell de `/profile` reprend le vocabulaire de la maquette du 06/08/2026 (« passe à Smart », « Essayer Smart ! », « ton forfait actuel · basique », clés `profile.upsell.*`), alors que `src/lib/subscription.ts` (`TIER_LABELS`), la page `/pricing` et les clés `profile.plan.*` parlent de Gratuit / Premium / Premium+. Un utilisateur qui clique « Essayer Smart ! » atterrit donc sur une page qui ne mentionne aucun « Smart ». Choix assumé à l'écriture (décision produit du 06/08/2026 : garder les noms de la maquette), à trancher avant toute mise en ligne de la tarification : soit renommer les offres partout, soit ramener l'encart au vocabulaire réel.

- **Questions sans correction automatique : aucune progression possible.** Une question de type texte libre, dessin ou fichier renvoie `correct: null` (le serveur ne sait pas trancher), donc `rewardCorrectAnswer` ne crédite rien — un chapitre composé uniquement de ces types ne progresse jamais, quel que soit le travail fourni. Piste retenue si le cas devient réel : après affichage de la correction, deux boutons « je savais / je ne savais pas » alimentant le score en auto-évaluation. Non implémenté faute de décision produit (06/08/2026).

- **Niveau de Bloom : un par question dans le modèle, un par notion dans la maquette.** L'éditeur de question posé sur la feuille d'examen (`InlineQuestionEditor.tsx`, 08/08/2026) affiche une pastille de niveau de Bloom sur chaque notion associée, comme la maquette `App-Culture.dc.html` — mais le modèle (`exam_questions.bloom_level`, contrainte `exam_questions_bloom_level_check`) n'en porte qu'un seul, au niveau de la question. Toutes les pastilles affichent donc la même valeur et cliquer l'une d'elles change le niveau de la question entière. À trancher : soit assumer un niveau par question (retirer la pastille des notions au profit d'un réglage unique), soit ajouter une colonne `bloom_level` à la table de jonction `exam_question_bricks` et faire suivre l'UI.

- **Barème avancé : deux commandes inertes.** Toujours dans `InlineQuestionEditor.tsx`, les paramètres avancés affichent deux boutons désactivés repris de la maquette et sans équivalent dans `QuestionWeight` : l'horloge (« calculer le score en fonction du temps de réponse », `gainMode: 'temps'` côté maquette) et le ⊖ (« inclure l'absence de réponse dans la pénalité », `penScope: 'toute'`). Même traitement que les boutons image/audio : la place est réservée, la fonction reste à spécifier.

- **Ordre aléatoire : annoncé sur la copie, pas encore appliqué.** Depuis le 17/08/2026, la gouttière gauche de la feuille A4 porte un repère (ⓘ `ShuffleNoticeIcon`, `tabs/examen/examShared.tsx`) en face de chaque ligne dont les éléments de réponse doivent changer d'ordre d'une copie à l'autre — paires (toujours), QCM et tableau (si « ordre aléatoire »). Le prédicat qui en décide est `shufflesAnswerItems` (`src/lib/workshops/examTypes.ts`). **Le mélange lui-même n'existe que dans le parcours** (`toChoices` dans `lib/workshops/exam.ts`, pour l'exercice interactif) : la feuille et le futur export impriment l'ordre saisi. Deux conséquences à traiter le jour de l'export (voir l'item « Export/passage réel des examens ») : (1) brancher le mélange sur `shufflesAnswerItems` — jamais une quatrième définition du même « quels types se mélangent » ; (2) pour les paires, mélanger **les deux colonnes indépendamment**, sinon l'élément reste en face de sa correspondance et la copie donne la réponse — c'est déjà le cas de l'aperçu aujourd'hui. Le tableau ne mélange que ses lignes (`tableShuffleRows`), jamais ses colonnes. **Point de jonction avec le parcours** : `toExerciseTypeOptions` (`lib/workshops/exam.ts`, PR #36) construit en liste blanche les réglages transmis au candidat, et `tableShuffleRows` n'y figure pas — un tableau réglé sur « ordre aléatoire » sort donc dans l'ordre saisi dans l'exercice interactif. Ce n'est pas une fuite de correction (le réglage ne porte aucune réponse), juste un réglage qui n'a pas encore d'effet côté parcours : l'ajouter à la liste blanche ne suffira pas, il faudra aussi mélanger les lignes dans `ExerciseClient`.

- **Sortir le barème de l'éditeur de question.** Le **modèle est déjà juste** : la pondération vit dans `ExamConfig.weighting` (`Record<questionId, QuestionWeight>`), portée par l'examen généré — ni `exam_questions` ni `exam_question_items` n'ont de colonne de points, et une même question peut donc valoir 1 point dans un contrôle et 3 dans un partiel. C'est l'**interface** qui mélange les deux : `QuestionFields` affiche les commandes de barème au milieu des champs de la question (points, malus, éliminatoire, dégressif), ce qui laisse croire que le barème est une propriété de la question. Décidé le 16/08/2026, non urgent. À faire : déplacer ce bloc hors du formulaire de question, du côté de la feuille d'examen (où il a du sens et où il est déjà indexé par question). Le repli `defaultWeight()` (`examen/examShared.tsx` — 1 point, sans malus, non éliminatoire) reste appliqué à la lecture partout (`config.weighting[id] ?? defaultWeight()`), donc aucune question ne peut se retrouver sans barème : rien à stocker sur la question pour s'en prémunir. L'éditeur du parcours, lui, n'affiche déjà aucun barème (`weight` non passé).

- **⚠️ Remise à zéro de progression — mécanisme de test temporaire à retirer.** `resetUserMastery` (`src/lib/workshops/mastery.ts`) + `resetMyParcoursProgress` (`src/app/actions/parcoursProgress.ts`) + le bouton « réinitialiser ma progression » de `ProgrammeTab.tsx` (clés i18n `programme.resetProgress*`). Ajouté le 15/08/2026 pour rejouer les mêmes questions pendant la mise au point du parcours sans avoir à en créer de nouvelles. Efface les lignes `brick_mastery` du **membre connecté** sur **cet atelier seulement** — l'identité vient de `requireMember`, jamais du client (une server action est une URL POST publique). À supprimer d'un bloc avant la mise en service, même discipline que l'allowlist Premium de `activateWorkshopPremium`. **Point d'attention** : la règle « un élève ne retombe jamais sur la même question » n'existe pas encore (`drawParcoursQuestion` tire au hasard et n'exclut que la question précédente) ; le jour où elle sera implémentée, son historique devra être purgé par cette même fonction, déjà pensée comme le point d'entrée unique de la remise à zéro.

- **`docs/backlog.md` est dupliqué.** Le corps du fichier apparaît deux fois (items repris à l'identique, deuxième en-tête « Dernière mise à jour » en milieu de fichier). Présent tel quel dans le dépôt, sans doute une mauvaise fusion. À dédoublonner — en vérifiant au passage que les deux derniers items, présents seulement dans la seconde copie, sont bien conservés.

- **Aligner l'interface sur `QuestionGroup` (sans migration).** Le stockage est symétrique depuis le 11/08/2026 (`exam_questions` = groupe, `exam_question_items` = questions) et la vue exposée à l'IA/API l'est aussi (`src/lib/workshops/questionGroup.ts`), mais l'interface parle encore `Question` (« question principale + `parts` ») : la conversion vit dans `exam.ts`. Deux scories en découlent, toutes deux **côté application uniquement — plus aucune migration n'est nécessaire** : (1) `ExamConfig.weighting` indexe le barème des questions liées par position (`<qid>::part<i>`, `partWeightKey` dans `tabs/examen/examShared.tsx`), d'où le décalage manuel des clés au retrait d'une question liée (`shiftPartWeights` dans `GeneratorContent.tsx`) — à remplacer par l'identifiant de la question, désormais stable ; (2) l'éditeur traite la première question à part alors qu'elle ne l'est plus. Chantier purement front, à faire quand l'éditeur sera retouché — ou par le prochain développeur, sans risque pour les données.

- **« Réponse orale » et « répondre sur l'image » : identifiées, pas encore branchées.** Deux modes de réponse ajoutés le 18/08/2026 dans les paramètres avancés de l'éditeur (`answerOnImage`, `oralAnswer` dans `QuestionTypeOptions`, `src/lib/workshops/examTypes.ts`). Ils sont saisis, stockés (jsonb `type_options`, aucune migration), transmis au candidat par `toExerciseTypeOptions` (`lib/workshops/exam.ts`) et exposés à l'IA via `QuestionGroup` — mais **rien ne les lit** : l'exercice du parcours affiche toujours une saisie écrite, et la feuille A4 les ignore volontairement (une copie papier n'a ni micro ni calque). À faire le jour du passage en ligne (voir l'item « Export/passage réel des examens ») : (1) « répondre sur l'image » → afficher l'image de la question comme support de réponse (annotation/dessin par-dessus) plutôt qu'un cadre à part, pour `dessin` et `liste` ; (2) « réponse orale » → **le format reste à spécifier** (enregistrement côté candidat, stockage, transcription, correction) — pour l'instant le drapeau ne fait qu'identifier ces questions, notamment pour la génération et la correction assistées par IA. Les deux sont exclusifs l'un de l'autre, exclusion tenue à la saisie (`setAnswerMode`, `tabs/examen/questionFields.tsx`). Deux détails à ne pas perdre : « répondre sur l'image » n'a de sens que si la question porte une image (le réglage n'est proposé que dans ce cas, mais la valeur stockée survit au retrait de l'image — toujours vérifier `image` avant d'en tenir compte), et l'éditeur en popup (`tabs/QuestionEditor.tsx`, plus monté nulle part) n'expose aucun `typeOptions` pour la question principale, donc ces réglages n'y apparaissent que sur les questions liées.
